import { Helmet } from "react-helmet-async";

const UpdateProfile = () => {
    return (
        <div>
            <Helmet> 
                <title>Update Profile</title>
            </Helmet>
            update your profile
        </div>
    );
};

export default UpdateProfile;