import { Helmet } from "react-helmet-async";

const UserProfile = () => {
    return (
        <div>
            <Helmet> 
                <title>User Profile</title>
            </Helmet>
            user profile
        </div>
    );
};

export default UserProfile;